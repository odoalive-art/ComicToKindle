appId: com.comictokindle.app
productName: ComicToKindle
directories:
  buildResources: build
files:
  - '!**/.vscode/*'
  - '!src/*'
  - '!electron.vite.config.{js,ts,mjs,cjs}'
  - '!{.eslintcache,eslint.config.mjs,.prettierignore,.prettierrc.yaml,dev-app-update.yml,CHANGELOG.md,README.md}'
  - '!{.env,.env.*,.npmrc,pnpm-lock.yaml}'
  - '!{tsconfig.json,tsconfig.node.json,tsconfig.web.json}'
  # 开发文件不进包
  - '!{AGENTS.md,components.json}'
  - '!{.claude,.gemini}/**'
  - '!{docs,design-system,.npm-cache}/**'
  - '!**/*.tsbuildinfo'
  # mac arm64 内测包只需 arm64 7za，剔除其他平台/架构二进制
  - '!node_modules/7zip-bin/{linux,win}/**'
  - '!node_modules/7zip-bin/mac/x64/**'
asarUnpack:
  - resources/**
  - node_modules/7zip-bin/**
  - node_modules/sharp/**
  - node_modules/@img/**
  - node_modules/@napi-rs/canvas/**
  - node_modules/@napi-rs/canvas-*/**
win:
  executableName: ComicToKindle
nsis:
  artifactName: ${name}-${version}-setup.${ext}
  shortcutName: ${productName}
  uninstallDisplayName: ${productName}
  createDesktopShortcut: always
mac:
  target:
    # dmg = 分发安装包；zip = electron-updater/Squirrel.Mac 自动更新用的增量来源
    - dmg
    - zip
  # 签名：走自定义钩子 scripts/sign-mac.cjs（electron-builder 自带签名器在本机会卡死，
  # 改用直接 codesign --deep）。传 CTK_SIGN_IDENTITY=<自签证书名> 用证书签（自动更新需要），
  # 不传则 ad-hoc。identity 必须是 "-" 才会进入自定义钩子；null 会在解析 sign 前
  # 直接跳过整个签名流程。钩子内不使用这个占位 identity，而是按环境变量签名。
  identity: "-"
  sign: scripts/sign-mac.cjs
  entitlementsInherit: build/entitlements.mac.plist
  extendInfo:
    - NSCameraUsageDescription: Application requests access to the device's camera.
    - NSMicrophoneUsageDescription: Application requests access to the device's microphone.
    - NSDocumentsFolderUsageDescription: Application requests access to the user's Documents folder.
    - NSDownloadsFolderUsageDescription: Application requests access to the user's Downloads folder.
  notarize: false
dmg:
  artifactName: ${name}-${version}-${env.BUILD_STAMP}.${ext}
  title: ${productName} ${version}
  writeUpdateInfo: false
  contents:
    - x: 130
      y: 220
      type: file
      path: dist/mac-arm64/ComicToKindle.app
      name: ComicToKindle.app
    - x: 410
      y: 220
      type: link
      path: /Applications
    - x: 270
      y: 360
      type: file
      path: build/release-notes-current.txt
      name: 版本说明.txt
linux:
  target:
    - AppImage
    - snap
    - deb
  maintainer: electronjs.org
  category: Utility
appImage:
  artifactName: ${name}-${version}.${ext}
npmRebuild: false
# 自动更新 feed：GitHub Releases。配了 publish 后即使本地 --publish never，
# electron-builder 也会生成 latest-mac.yml（electron-updater 据此发现新版）。
publish:
  provider: github
  owner: odoalive-art
  repo: ComicToKindle
